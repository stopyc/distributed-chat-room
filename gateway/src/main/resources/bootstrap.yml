spring:
  application:
    name: gateway # 服务名称
  cloud:
    nacos:
      server-addr: ${NACOS_HOST} # nacos地址
#      server-addr: localhost # nacos地址
      discovery:
#        ephemeral: false
        # 广州集群
        cluster-name: GZ