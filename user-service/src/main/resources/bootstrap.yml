spring:
  cloud:
    inetutils:
      preferred-networks:
        - 192.168.31
        - 47.113
    nacos:
      server-addr: ${NACOS_HOST} # nacos地址
      config:
        file-extension: yaml # 文件后缀名
      discovery:
        # 广州集群
        cluster-name: GZ
        ephemeral: true
        ip: 123.207.51.46

